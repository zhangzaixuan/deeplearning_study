import math

# I need it,but I am on a version with 3.9,it's so bad

"""
import latexify
@latexify.with_latex#调用latexify的装饰器
def solve(a, b, c):
  return (-b + math.sqrt(b**2 - 4*a*c)) / (2*a)

solve
print('\pm;')
"""
